import { motion } from 'framer-motion';
import { ShieldCheck, HelpCircle, BarChart, Info, Layers } from 'lucide-react';

export function ExplainabilityPage() {
  const shapFeatures = [
    { name: 'Recent 3-Hour Emergency Admissions', importance: '+0.38', color: '#DC2626' },
    { name: 'Monsoon Heavy Rainfall Warning', importance: '+0.24', color: '#D97706' },
    { name: 'Weekend Evening Time Window', importance: '+0.18', color: '#8B5CF6' },
    { name: 'Regional Flu Outbreak Severity Index', importance: '+0.12', color: '#0EA5E9' },
    { name: 'General Ward Discharge Speed', importance: '-0.09', color: '#16A34A' },
  ];

  return (
    <div className="space-y-8">
      {/* Header */}
      <div className="flex flex-col md:flex-row md:items-center justify-between gap-4">
        <div>
          <div className="flex items-center gap-2 mb-1">
            <div className="w-8 h-8 rounded-xl bg-violet-500/10 dark:bg-violet-500/20 flex items-center justify-center text-violet-600 dark:text-violet-400">
              <ShieldCheck className="w-4 h-4" />
            </div>
            <h1 className="text-2xl font-bold text-slate-800 dark:text-navy-100 tracking-tight">
              AI Prediction Explainability & SHAP Values
            </h1>
          </div>
          <p className="text-sm text-slate-500 dark:text-navy-400">
            Understand why the model predicts emergency surges with transparent feature contribution analysis.
          </p>
        </div>
      </div>

      {/* SHAP Feature Contribution Chart */}
      <div className="glass-strong rounded-2xl p-6 border border-slate-200/60 dark:border-white/5">
        <h3 className="text-base font-bold text-slate-800 dark:text-navy-100 mb-4">
          SHAP Feature Impact Breakdown (Top Predictors for Current Surge)
        </h3>

        <div className="space-y-4">
          {shapFeatures.map((f) => (
            <div key={f.name} className="space-y-1.5 text-xs">
              <div className="flex justify-between items-center font-medium">
                <span className="text-slate-700 dark:text-navy-200">{f.name}</span>
                <span className="font-bold tabular-nums" style={{ color: f.color }}>
                  SHAP Impact: {f.importance}
                </span>
              </div>
              <div className="h-3 rounded-full bg-slate-100 dark:bg-navy-800 overflow-hidden">
                <div
                  className="h-full rounded-full transition-all"
                  style={{
                    width: `${Math.abs(parseFloat(f.importance)) * 200}%`,
                    backgroundColor: f.color,
                  }}
                />
              </div>
            </div>
          ))}
        </div>
      </div>

      <div className="glass-card rounded-2xl p-6 flex items-start gap-4">
        <Info className="w-5 h-5 text-accent-500 shrink-0 mt-0.5" />
        <div className="text-xs text-slate-600 dark:text-navy-300 leading-relaxed">
          <strong className="text-slate-800 dark:text-navy-100 block mb-1">Explainable AI Assurance</strong>
          All predictions generated by EWI undergo SHAP (SHapley Additive exPlanations) verification to provide clinical staff with actionable reasons behind every surge flag, preventing false alarms and building trust.
        </div>
      </div>
    </div>
  );
}
